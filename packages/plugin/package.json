{
  "name": "plugin",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.104.0"
  }
}
